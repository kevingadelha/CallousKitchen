<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.callouskitchenandroid">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/DarkTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".EatFoodActivity" android:label="Eat"></activity>
        <activity android:name=".DeleteFoodActivity" android:label="Throw Away" />
        <activity android:name=".AddFoodActivity" android:label="Add Food" />
        <activity android:name=".EditFoodActivity" android:label="Edit Food" />
        <activity android:name=".CreateAccountActivity" android:label="Create Account" />
        <activity android:name=".AddKitchenActivity" android:label="Add Kitchen" />
        <activity
            android:name=".InventoryActivity"
            android:label="Inventory" />
        <activity
            android:name=".KitchenListActivity"
            android:label="My Kitchens" />
        <activity
            android:name=".MainActivity"
            android:label="Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>